#pragma once

enum class Direction {
    UP,
    DOWN,
    RIGHT,
    LEFT,
};

constexpr int screenWidth = 40;
constexpr int screenHeight = 40;

constexpr float tileSize = 20;
constexpr float fruitBorder = 40;
